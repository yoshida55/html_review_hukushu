Useful Windows commands for this project:
- `Get-ChildItem` to list files
- `rg -n "pattern" .` to search text quickly
- `Get-Content .\path\file | Select-Object -First N` to inspect files
- `git status --short` to check local changes
Run the project by opening `index.html` in a browser or using a local static server if needed. No dedicated test, lint, or format commands were found.